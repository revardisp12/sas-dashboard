import type { Brand } from '@/lib/types'

export default function KontenTab({ brand }: { brand: Brand }) {
  return <p style={{ color: '#6B7280', fontSize: 13 }}>Konten KOL — coming soon ({brand})</p>
}
